openapi: 3.0.0

info:
  title: SupportIQ API
  version: 1.0.0

servers:
  - url: http://localhost:5000

paths:

  /api/v1/health:
    get:
      summary: Health Check

      responses:
        '200':
          description: OK

  /api/v1/auth/login:
    post:
      summary: Login

      requestBody:
        required: true

      responses:
        '200':
          description: Logged in