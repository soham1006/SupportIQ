version: "3.9"

services:
  chromadb:
    image: chromadb/chroma:latest

    container_name: supportiq-chromadb

    ports:
      - "8000:8000"

    volumes:
      - chroma_data:/chroma/chroma

    restart: unless-stopped

volumes:
  chroma_data: